## Destination

Spec buildável de um app Windows (WPF .NET 8) com paridade total do Copyous — tipos, pins, tags, ações, atalhos, configs, estilo + botão no system tray — pronta para `to-spec`.

## Notes

- Stack trancada: **C# WPF (.NET 8)**. Solo Windows-only, sem shim.
- Hotkeys default: **abrir `Ctrl+Shift+V`**, **incognito `Ctrl+Shift+Alt+V`**, configuráveis. `Win+V` / `Win+Shift+V` fora (reservados do SO).
- Tray: clique abre popup; menu = `Abrir / Incognito / Limpar (manter pins+tags) / Configs / Sair`.
- Distribuição: **portable single-file + instalador Inno Setup** (MSIX depois). Autostart via chave `Run`. Single-instance via `Mutex` nomeado. Alvo **Win10 20H2+ / Win11**.
- Mapa de equivalências Linux→Windows trancado: `Libgda→Microsoft.Data.Sqlite`, `GSound→System.Media`, `DBus→tray+CLI/named-pipe local`, `Soup→HttpClient`, `hljs→highlight local`, `paste virtual→SendInput`, `GSettings→JSON local`, `nautilus/xdg-open→ShellExecute`.
- Licença: **GPL-3.0-or-later** (igual ao original).
- Skills por ticket: `grilling` + `domain-modeling` em todo HITL; `research` via subagente em AFK; `prototype` para dúvida de UI/comportamento.
- Tracker: local markdown. Frontier = `issues/` abertas, desbloqueadas, unclaimed. `Status: claimed` antes de trabalhar; `Status: resolved` + `## Answer` ao resolver.
- Fonte original: https://github.com/boerdereinar/copyous (8 tipos: Text, Code, Image, File, Files, Link, Character, Color; DB v2; 9 tags; actions.json; 4 páginas prefs; DBus Toggle/Show/Hide/ClearHistory).

## Decisions so far

- [Stack WPF .NET 8](issues/01-research-copyous-parity-inventory.md): decisão de charting Q3 — sem shim para `AddClipboardFormatListener` + `CF_HDROP` + `NotifyIcon` + `Microsoft.Data.Sqlite`. (contexto: esta sessão de charting)
- [Hotkeys + tray defaults](issues/03-research-hotkeys-tray-popup.md): decisão de charting Q4 — `Ctrl+Shift+V` / `Ctrl+Shift+Alt+V` + menu tray de 5 itens. (contexto: esta sessão de charting)
- [Portable + Inno](issues/02-research-windows-clipboard-core.md): decisão de charting Q5 — Win10 20H2+/Win11, single-file + Inno, `Run` + `Mutex`. (contexto: esta sessão de charting)
- [Mapa de equivalências](issues/01-research-copyous-parity-inventory.md): decisão de charting Q6 — trancado acima. (contexto: esta sessão de charting)
- [GPL-3.0-or-later](issues/08-grilling-distribuicao-licenca.md): decisão de charting Q7 — manter. (contexto: esta sessão de charting)
- [Inventário de paridade Copyous](issues/01-research-copyous-parity-inventory.md): 8 tipos + 9 tags hex + SQLite v2 + pipeline Image>File>Text + classificação pura + actions.json + ~70 prefs + DBus; difícil = monitor Win32, thumbnails, UI, GSound, parser de cor. Detalhe em `research/01-parity-inventory.md` (commit fa103e3). Desbloqueia 04, 05, 06.
- [Núcleo clipboard Windows](issues/02-research-windows-clipboard-core.md): `AddClipboardFormatListener`+`WM_CLIPBOARDUPDATE` (HwndSource/message-only), sem polling; `CF_HDROP`+`Preferred DropEffect`, DIB→PNG, `CF_HTML` opaco v1; `SendInput`+UIPI com fluxo captura-alvo→assert→injeta; `Mutex Local\`+handoff pipe, `Run` autostart; gap Tauri/Electron confirmado. Detalhe em `research/02-clipboard-core.md`. Spikes (terminal, delay, FileDescriptor) adiados p/ `to-tickets`. Desbloqueia 08.
- [Hotkeys + tray + popup](issues/03-research-hotkeys-tray-popup.md): `RegisterHotKey` ids `0x0000–0xBFFF` + `NOREPEAT`, `Win+V` fora; **`H.NotifyIcon.WPF`** escolhido; esquerdo toggle/direito menu 5 itens; popup `ShowActivated=False`, `Deactivated→Hide()`, cursor-first v1 + clamp DPI, caret UIA v2; **desvio: toggle-pinned `Alt`→`Alt+P`** (WPF `Key.System`). Detalhe em `research/03-hotkeys-tray-popup.md`. Desbloqueia 07, 08.
- [Persistência + busca](issues/04-research-persistencia-busca.md): **`Microsoft.Data.Sqlite`** (EF rejeitado); upsert `ON CONFLICT` bumpeando datetime; `clear`/`deleteOldest` com proteção; metadata `System.Text.Json` (corrompido→null); imagens/cache em `%LocalAppData%` + GC órfãos; SQLite+Memory-testes, JSON fora; **`Sync Primary` N/A permanente**. Detalhe em `research/04-persistencia-busca.md`. Aberto p/ spec: STRICT, formato datetime, fusão track().
- [Ações + incognito + previews](issues/05-research-acoes-incognito-previews.md): actions.json 1:1 em `%AppData%`, regex .NET documentado + timeout 2s; libs **QRCoder 1.8.0 / AngleSharp 1.7.2 / AvalonEdit 6.3.1** (todas MIT); incognito em memória; `SessionEnding` cleanup <1s + CLI `--clear/--clear-all`; colar `Ctrl+V` default; preview HttpClient 5s; sons v1 `MediaPlayer`; **toast bloqueado até AUMID** → balão tray; pipe `Local\WindowsCM.<sid>`. Detalhe em `research/05-acoes-incognito-previews.md`.
- [Configs + customizações](issues/06-grilling-configs-customizacoes.md): cortes (Yaru, WM_CLASS→processo, indicator-display; Dependencies→Sobre/Diagnóstico); profiles Default+Compact; History UI só-SQLite; **UI inglês v1**, item/entry em `CONTEXT.md` (7 termos); 9 sons; 6 telas por-tipo; first-run Default; middle-click pin + toggle + swaps mantidos. Sem ADRs.
- [Protótipo popup](issues/07-prototype-popup-estilo.md): veredito **A (Cards)**; spec leva densidade Copyous 1:1, **Dark**, sem vazios verticais, largura cheia. Asset na branch `prototype/popup-wpf` (`a314624` + `817f0c2`). B/C descartadas.
- [Distribuição + licença](issues/08-grilling-distribuicao-licenca.md): Inno per-user + autostart opt-in + uninstall preserva dados; GPL-3.0 (LICENSE + SPDX + Sobre com créditos); **readiness confirmado — mapa done, handoff para `to-spec` autorizado**.
- [Clique simples cola](issues/23-clique-simples-cola.md): single-click em item do popup cola via `PopupClickPolicy.ShouldActivate(isVisible, index)` + `PreviewMouseLeftButtonUp` (MouseUp evita falso disparo em scroll/drag); Shift+click só copia (`shiftHeld: true`); teclado (setas/Home/End/slots) navega sem colar; duplo-clique idempotente via gate `_isActivating`. Smoke automatizado no `smoke-ui.ps1` com execução no `WinSta0\default`.
- [Tray na gaveta do Windows 11 + onboarding](issues/24-tray-gaveta-onboarding.md): confirmação do ícone na gaveta de overflow do Windows 11 com menu de 5 itens funcional, feedback de cópia (flash 3x65ms + balão), onboarding no Settings/Diagnóstico orientando o usuário a arrastar o ícone para a taskbar (sem tentar promoção programática contra as regras da plataforma). Smoke automatizado no `smoke-ui.ps1` captura telas full e cropped da gaveta aberta e valida observabilidade de ponta a ponta.
- [Cleanup dos logs temporários + relatório final do smoke](issues/25-cleanup-relatorio-smoke.md): remoção total de `TempSmokeLog`, testes associados e `smoke-ui.ps1`; gravação zero em `%TEMP%` em tempo de execução; suíte com 735 testes verdes e zero warnings/erros; relatório final de validação (`smoke-report.md`) consolidando popup 1080p determinístico em 4 quadrantes, Notepad (Enter/Shift+Enter/clique/Shift+clique), UIPI com alvo elevado, tray na gaveta e lista de follow-ups pós-MVP.
- [Redesign horizontal cards + Windows 11 Fluent](issues/26-redesign-horizontal-cards-win11-media-preview.md): retomada da Variante A (Cards) do protótipo com popup horizontal 880x320px; isolamento de `ItemDisplayFormatter` em Core; formatação sem caminhos absolutos para arquivos e imagens; miniaturas para imagens locais do Explorer; previews expandidos de código monoespaçado (6-8 linhas); botões com ícones Segoe Fluent Icons e estilo Windows 11. ADR 0001 criado.
- [Largura total + scroll suave + PT-BR + card menu + emoji](issues/27-popup-full-width-smooth-scroll-ptbr-card-menu-emoji.md): popup horizontal preenchendo a tela (*horizontal fill*) com respiro; scroll contínuo suave por pixel (*ease-out* 200ms); localização 100% PT-BR; botões de fixar, menu `...` e lixeira no cabeçalho do card com menu contextual completo; exibição enriquecida de emojis com `Segoe UI Emoji` 54px centralizado e codepoint Unicode.
- [Abertura instantânea tray + tema claro Windows](issues/28-instant-tray-open-and-windows-light-theme.md): eliminação total do timer de 500ms do `TrayManager` com abertura instantânea (0ms) no clique simples; duplo clique seguro mantendo o popup aberto; proteção contra foco falso do Explorer (`OnDeactivated` < 250ms) e coordenação de fechamento (`WasRecentlyHidden` < 350ms); suporte nativo e dinâmico ao tema claro do Windows 11 com detecção automática via `AppsUseLightTheme` no Registro e paleta Fluent (`#F3F3F3` fundo, `#FFFFFF` cards, código `#F6F8FA`, alto contraste).
- [Botões do card + frame rate da rolagem + barra de rolagem](issues/29-card-menu-actions-framerate-scrollbar-layout.md): isolamento de cliques interativos no card via `PopupClickPolicy` (evitando ativação/fechamento indevido nos botões de Pin, Opções e Excluir); proteção contra desativação ao abrir `ContextMenu` e diálogos (`PopupDeactivationPolicy`); rolagem suave atrelada ao V-Sync nativo do monitor (120Hz/144Hz/240Hz) via `SmoothScrollController` e `CompositionTarget.Rendering` sem engasgos; altura aumentada para 348px (`PopupSizing.MaxHeight = 348`) com barra de rolagem Fluent slim eliminando qualquer sobreposição ou corte nos cards.
- [Ícones de sistema para arquivos + syntax highlighting para códigos](issues/30-file-system-icons-and-code-syntax-highlighting.md): exibição rica de arquivos copiados via `FileIconService` (Shell32 `SHGetFileInfoW` com cache thread-safe e liberação de handles), `FileDisplayHelper` (tamanhos formatados, nomes amigáveis e resumo multi-arquivo) e caso dedicado `FilePreviewVisibility` eliminando caminhos absolutos crus; destaque léxico de sintaxe para códigos via `CodeSyntaxTokenizer` com palavras reservadas, tipos, strings e comentários coloridos dinamicamente nos temas Claro e Escuro do Windows 11 via `SyntaxHighlightHelper`; 798 testes unitários 100% verdes.
- [Favicons de websites em links + separação sutil de cores por tipo de item](issues/31-link-favicons-and-item-type-accent-colors.md): exibição rica de websites copiados via `FaviconService` (cache em 2 níveis RAM/disco em `%LocalAppData%\WindowsCM\favicons\` com download assíncrono não bloqueante via CDN Google Favicons sz=64 e notificação reativa `FaviconUpdated`), `LinkDisplayHelper` (extração pura de domínio, títulos e URLs amigáveis) e caso dedicado `LinkPreviewVisibility` com container arredondado de 46x46 px e fallback Fluent `\uE774`; separação sutil e elegante por cores para cada tipo de item via `ItemTypeTheme` (paleta semântica Fluent Claro/Escuro para Link, Code, File, Image, Character, Color e Text), ícone de tipo colorido no cabeçalho, indicador lateral com fallback de cor de tipo quando sem tag e badge/pílula Fluent inferior com cantos arredondados e fundo translúcido; 827 testes unitários 100% verdes.
- [Redesign das configurações + limite 100 de histórico + limpar cache + cores por tipo](issues/32-settings-redesign-clipboard-limit-cache-clean-item-colors.md): janela de configurações reformulada no padrão Windows 11 Fluent Design (duas colunas com sidebar de navegação por categorias, suporte dinâmico a tema Claro e Escuro); limite de histórico configurado para 100 como padrão e teto máximo via `SettingLimits` com slider e truncamento imediato via `_store.Evict`; módulo `CacheCleaner` em Core para exclusão segura de arquivos temporários e contagem em tempo real de arquivos e bytes liberados; personalização de cores semânticas por tipo de item (`ItemColorSettings`) integrada a `ItemTypeTheme` e `PopupThemeBrushes` com amostras de badge em tempo real, seletor de cor e redefinição para os padrões Fluent; 839 testes unitários 100% verdes.
- [Renderização rica de emojis e múltiplos emojis](issues/33-color-emoji-thumbnail-and-multi-emoji-classifier.md): detecção pura de emojis via `EmojiDetector` garantindo que múltiplos emojis puros sejam classificados como `ItemKind.Character` (Emoji) e preservando textos mistos como `ItemKind.Text`; apresentação no card com contagem e códigos Unicode; renderização nativa de miniaturas de emoji full-color preenchidas via Direct2D / DirectWrite com a fonte oficial Segoe UI Emoji do Windows 11.
- [Redesign da top bar com simetria e elegância do Copyous](issues/34-topbar-redesign-copyous-symmetry.md): substituição da top bar assimétrica por layout de 3 colunas (`1*`, `Auto`, `1*`) com centralização matemática perfeita da cápsula de busca (420x34px, `CornerRadius="17"`); botões circulares de Configurações (`⚙`) e Incognito (`🔕`) à esquerda equilibrando o botão pílula Limpar (`Clear`) à direita; seletor dropdown de tipos `🔍 ▾` e botão de fixados `📌` integrados diretamente dentro da cápsula de busca; marca d'água fluida "Digite para pesquisar..."; suporte dinâmico completo aos temas Claro e Escuro; 887 testes unitários 100% verdes.
- [Modo anônimo efêmero, limpeza total e feedback visual](issues/35-incognito-ephemeral-session-and-visual-feedback.md): sessão anônima isolada em memória (`:memory:`) com limpeza imediata e irreversível de clipes e mídias temporárias ao desativar; sincronização unificada de estado entre atalhos, menu de bandeja e popup; feedback visual marcante no padrão Windows 11 Fluent (banner MODO ANÔNIMO ATIVO com botão de saída rápida, brilho/borda superior roxo InPrivate, botão estilizado e card de estado vazio).
- [Resolução de conflitos do modo anônimo (tray, atalhos e proteção de sessão)](issues/36-incognito-conflicts-tray-hotkeys-session-protection.md): invariante de não-destrutividade por visibilidade (abrir, fechar, alternar com atalho ou clicar na bandeja jamais encerra a sessão efêmera nem apaga dados); remoção de efeito colateral destrutivo em `Show()`; tray e atalhos globais `Ctrl+Shift+V` e `Ctrl+Shift+Alt+V` com suporte a toggle limpo; suporte a dual-view no popup (visualização do histórico persistente normal sem destruir a sessão anônima ativa).



## Not yet specified

_(vazio — fog zerado em 07; o que resta é `08`, já tickado.)_

## Out of scope

- Sync nuvem / multi-device.
- Plugins de terceiros / store de extensões.
- MSIX/Store dia 1 (adiado para pós-MVP da spec).
- Port para macOS/Linux (só Windows neste esforço).
- Caret-UIA cursor mode (cursor-first v1 confirmado em 03/07; caret global vira v2 pós-spec).
- Toast real (bloqueado até installer com AUMID; balão tray na v1 — ver 05).

## Out of scope

- Sync nuvem / multi-device.
- Plugins de terceiros / store de extensões.
- MSIX/Store dia 1 (adiado para pós-MVP da spec).
- Port para macOS/Linux (só Windows neste esforço).
